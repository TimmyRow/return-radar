# Return Radar

A private, local-first deadline dashboard for return windows and warranties. Add a purchase, see urgency at a glance, and decide before a deadline costs you money.

Run `npm run check` to verify the calculation engine. Serve locally with `python -m http.server 8000`.
